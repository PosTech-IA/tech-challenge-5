[tool.uv.workspace]
members = [
    "shared",
    "services/gateway",
    "services/upload",
    "services/processor",
    "services/reports",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.pytest.ini_options]
testpaths = ["tests"]
